/* 23. Crea un programa que muestre en la consola la tabla de multiplicar del 5 (desde 1 hasta 
10) utilizando un bucle `for`.
 */

// Variable
const cinco = 5;

// Result
for (let i = 1; i <= 10; i++) {
    console.log(`${i} x ${cinco} = ${i*cinco}`);
}